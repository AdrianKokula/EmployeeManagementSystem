<!-- Copyright (c) 2020 Adrián Kokuľa - adriankokula.eu; License: The MIT License (MIT) -->
	
<Page x:Class="EmployeeManagementSystem.Pages.Employees"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	  xmlns:local="clr-namespace:EmployeeManagementSystem.Pages"
	  mc:Ignorable="d" FontSize="16" FontFamily="Bahnschrift Light"
	  d:DesignHeight="450" d:DesignWidth="800"
	  Title="Employees">

	<Grid Name="GridMain">

		<Grid.Resources>
			<Style x:Key="GridViewColumnsStyle" TargetType="{x:Type GridViewColumnHeader}">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
							<TextBlock Text="{TemplateBinding Content}" Margin="0 10" Foreground="White" />
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</Grid.Resources>
		
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Background="#FFAA4D">

			<DockPanel Margin="10" LastChildFill="False">

				<StackPanel Orientation="Horizontal">
					<Image Source="../Resources/Employee.png" MaxHeight="32" Margin="5 0"/>
					<TextBlock Grid.Column="1" FontSize="20" Text="Employees" VerticalAlignment="Center" Foreground="White"/>
				</StackPanel>

				<Button Grid.Column="2" Name="BtnAdd" Padding="0" DockPanel.Dock="Right" Cursor="Hand" Click="BtnAdd_Click" BorderThickness="0" Background="Transparent">
					<StackPanel Orientation="Horizontal">
						<Image Source="../Resources/Add.png" MaxHeight="32" MaxWidth="32" Margin="5 0"/>
						<TextBlock Text="Add" Foreground="White" VerticalAlignment="Center"/>
					</StackPanel>
				</Button>

			</DockPanel>

		</Grid>

		<ListView Grid.Row="1" Name="LvEmployees" SelectionChanged="LvEmployees_SelectionChanged" BorderThickness="0 3" Background="#333b41" Foreground="White">

			<ListView.ItemContainerStyle>
				<Style TargetType="{x:Type ListViewItem}" >
					<Setter Property="Height" Value="48" />
					<Setter Property="Margin" Value="0 5" />
					<Setter Property="Background" Value="#3f474e" />
					<Setter Property="Cursor" Value="Hand" />
				</Style>
			</ListView.ItemContainerStyle>
			
			<ListView.View>
				<GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnsStyle}">
					<GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding ID}" />
					<GridViewColumn Header="First name" Width="150" DisplayMemberBinding="{Binding FirstName}" />
					<GridViewColumn Header="Last name" Width="150" DisplayMemberBinding="{Binding LastName}" />
					<GridViewColumn Header="Department" Width="225" DisplayMemberBinding="{Binding Department}" />
					<GridViewColumn Header="Date of birth" DisplayMemberBinding="{Binding DateOfBirth}" />
					<GridViewColumn Header="Residence" DisplayMemberBinding="{Binding Residence}"/>
				</GridView>
			</ListView.View>
			
		</ListView>

	</Grid>
</Page>
